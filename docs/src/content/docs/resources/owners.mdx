---
title: Owners
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

## Overview

## Methods

### Get a single owner

```py
get(
  owners_id: int
  ) -> OwnersResponse
```

| argument | type |
| -------- | ---- |
| owners_id | `int` |

#### Example

<Tabs syncKey="client">
  <TabItem label="sync">
    ```py
    from openaq import OpenAQ

    with OpenAQ() as client:
        client.owners.get(owners_id=1)
    ```
  </TabItem>
  <TabItem label="async">
    ```py
    from openaq import AsyncOpenAQ

    async with AsyncOpenAQ() as client:
        await client.owners.get(owners_id=1)
    ```
  </TabItem>
</Tabs>

### List multiple owners

```py
list(
  page: int = 1,
  limit: int = 1000,
  order_by: str | None = None,
  sort_order: SortOrder | None = None
  ) -> OwnersResponse
```

| argument | type | description |
| -------- | ---- | ----------- |
| page | `int` | The page number, must be greater than zero. Page count is owners found / limit. |
| limit | `int` | The number of results returned per page. Must be between 1 and 1,000. |
| order_by | `str \| None` | Order by operators for results. |
| sort_order | `SortOrder \| None` | Order for sorting results (asc/desc). |

#### Example

<Tabs syncKey="client">
  <TabItem label="sync">
    ```py
    from openaq import OpenAQ

    with OpenAQ() as client:
        client.owners.list()
    ```
  </TabItem>
  <TabItem label="async">
    ```py
    from openaq import AsyncOpenAQ

    async with AsyncOpenAQ() as client:
        await client.owners.list()
    ```
  </TabItem>
</Tabs>

## Response

```
Owner[]
    ├── id: int
    └── name: str
```