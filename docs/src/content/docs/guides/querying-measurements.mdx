---
title: Querying measurements
description: Querying historical measurements from a monitoring station
---
import FeedbackForm from '@components/FeedbackForm.astro'
import { Aside, Steps } from '@astrojs/starlight/components';


A common task with the OpenAQ API is fetching historical measurements from one
or more monitoring stations. This guide covers the basics:

> What was the daily average PM<sub>2.5</sub> in Accra, Ghana for 2025?

This guide assumes you have installed the OpenAQ Python SDK and have a valid
OpenAQ API key.

## Steps

The OpenAQ API works top-down. For this task, that means moving through three
resources: Locations, Sensors, and Measurements. The steps are:

1. Find the IDs of monitoring stations within the city of Accra.
2. Pick one location, then use the Locations resource with that location's ID to
find the ID of the sensor measuring PM<sub>2.5</sub> at that location.
3. Use the Measurements resource with that sensor's ID to get data at the
granularity and timeframe you want.

Below is the step-by-step workflow with code you can run in a Python notebook.

<Steps>

1. Instantiate the client.

    ```py
    from openaq import OpenAQ
    from datetime import date

    client = OpenAQ(api_key='replace-me-with-a-valid-key')
    ```

    <Aside type="caution">
    This example use a placeholder API key `replace-me-with-a-valid-key` and 
    will not work as-is. Replace this value with a valid OpenAQ API key.
    </Aside>


2. Retrieve the ID of one location in Accra, Ghana. Search 10 km (10,000 meters)
    around Accra's central coordinates, 5.54986, -0.21068, and pick the first
    result for simplicity.

    ```py
    accra_locations = client.locations.list(
        coordinates=(5.5583456, -0.2043158),
        radius=10000, # in meters
        limit=1000
    ).results

    locations_id = accra_locations[0].id
    ```

3. Retrieve the ID of the sensor that measures PM<sub>2.5</sub> at the chosen
    location.


    ```py
    sensors = client.locations.sensors(locations_id)
    pm25_sensors_id = 0

    for sensor in sensors_resp.results:
        if "pm25" in sensor.name:
            pm25_sensors_id = sensor.id
            break
    ```

4. Fetch the daily average measurements.

    ```py
    response = client.measurements.list(pm25_sensors_id,
                                        data="days",
                                        date_from=date(2025, 1, 1),
                                        date_to=date(2025, 12, 31)
                                        )

    measurements = response.results

    client.close()

    print(measurements)
    ```

    You will now have the daily measurements for 2025 at the chosen location in
    Accra stored as a list in `measurements` and printed to your console.

</Steps>

While you can get the results set of this example in the first page of the
response (i.e. we are expecting 365 records in the results of this query), there
will be instances where the records will be spread across several pages when you
query at different granularity levels (for example `hours`) or over a longer period of
time. In those cases, it will be necessary to [page the
results](/guides/paging-results) to get the full data you request.

Now that you have the measurements, if you want to work with the datasets using
a data frame library like [Pandas](https://pandas.pydata.org/), we have a
tutorial on [basic pandas integration](/guides/pandas). We also have a guide
where you can instead [output the data to a .csv file](/guides/write-to-csv) for
later use.

<FeedbackForm/>